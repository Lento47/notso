Role: Backend + UI Engineer.
Goal: build a minimal HTTP server with endpoints and a static UI.
Deliverables:
- /search endpoint (JSON)
- / endpoint (HTML UI)
- /static assets serving
- tests (at least basic request parsing unit tests)
Rules:
- Standard library only.
- Keep it small and readable.
